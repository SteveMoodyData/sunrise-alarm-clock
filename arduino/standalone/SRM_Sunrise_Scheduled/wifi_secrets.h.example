// Template for wifi_secrets.h, which is gitignored and never committed.
// Copy this file to wifi_secrets.h (same folder) and fill in your real
// network credentials before flashing SRM_Sunrise_Scheduled.ino.
#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-network-password"
